# =============================================================================
# T&A Indien Rejsedagbog - Environment Variables
# =============================================================================
# Kopiér denne fil til .env.local og udfyld værdierne
# cp .env.example .env.local
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration (påkrævet)
# -----------------------------------------------------------------------------
# Find disse værdier i dit Supabase projekt under Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -----------------------------------------------------------------------------
# Mapbox Configuration (påkrævet for kortvisning)
# -----------------------------------------------------------------------------
# Opret en konto på https://www.mapbox.com/ og find din access token
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your-public-token

# -----------------------------------------------------------------------------
# Site URL (påkrævet for auth redirects)
# -----------------------------------------------------------------------------
# Din apps URL - bruges til magic links og password reset
# I udvikling: http://localhost:3000
# I produktion: https://din-domain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Admin/Author Emails (valgfri)
# -----------------------------------------------------------------------------
# Komma-separeret liste over emails der automatisk får author-rettigheder
# Disse brugere kan oprette og redigere opslag
# Eksempel: admin@example.com, author@example.com
ADMIN_EMAILS=

# -----------------------------------------------------------------------------
# Journey Configuration (valgfri)
# -----------------------------------------------------------------------------
# Startdato for rejsen (Dag 0) i ISO-format
# Bruges til beregning af "Dag X" labels i feed
# Standard: 2025-12-18 hvis ikke angivet
NEXT_PUBLIC_JOURNEY_START_DATE=2025-12-18

# -----------------------------------------------------------------------------
# Upload Configuration (valgfri)
# -----------------------------------------------------------------------------
# Maksimal filstørrelse for uploads i MB
# Standard: 500 MB hvis ikke angivet
NEXT_PUBLIC_MAX_FILE_SIZE_MB=500

# Antal parallelle uploads (1-5)
# Lavere værdi = mere stabil, højere = hurtigere
# Standard: 3 hvis ikke angivet
NEXT_PUBLIC_UPLOAD_CONCURRENCY=3
